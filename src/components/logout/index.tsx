import React from "react";
import { StyleSheet, Text, View } from "react-native";

const LogoutIndex = () => {
  return (
    <View>
      <Text>LogoutIndex</Text>
    </View>
  );
};

export default LogoutIndex;

const styles = StyleSheet.create({});
